// For listing pages
export function updateTitle(listingTitle) {
  document.title = `AuctionVintageItems | ${listingTitle}`;
}

// For profile pages
export function updateProfileTitle(username) {
  document.title = `AuctionVintageItems | ${username} - Profile`;
}
